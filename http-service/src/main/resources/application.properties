spring.config.import=classpath:common.properties
server.port=10080
postgres.schema=http_service
logging.level.org.springframework.web=TRACE
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://${KEYCLOAK_HOST}:${KEYCLOAK_PORT}/realms/${KEYCLOAK_REALM}
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_HOST}:${KEYCLOAK_PORT}/realms/${KEYCLOAK_REALM}/protocol/openid-connect/certs
